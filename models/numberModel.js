// models/numberModel.js — Arquivista placeholder

// Aqui é onde ficarão os esquemas de dados se usarmos banco no futuro
// (exemplo: MongoDB + Mongoose)

// Por enquanto, este arquivo não é usado, mas já está preparado para crescer!

// Exemplo futuro:
// const mongoose = require('mongoose');
// const numberSchema = new mongoose.Schema({
// numeros: [Number]
// });
// module.exports = mongoose.model('NumberModel', numberSchema);
